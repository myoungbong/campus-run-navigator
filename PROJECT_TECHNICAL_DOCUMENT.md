# Campus Run Navigator 기술 문서

## 1. 프로젝트 개요

Campus Run Navigator는 충북대학교 캠퍼스 내부에서 러닝 경로를 추천하고, 직접 측정한 GNSS 이동 경로를 지도 위에 시각화하는 웹 기반 프로젝트이다. 사용자는 목표 거리, 러닝 페이스, 경사도 난이도, 출발점, 도착점을 설정할 수 있으며, 시스템은 실측 GNSS 데이터를 기반으로 저장된 대표 노드 그래프에서 조건에 맞는 러닝 코스를 추천한다.

이 프로젝트는 위성항법시스템 과목에서 U-blox ZED-F9P 수신기를 활용해 직접 수집한 위치 데이터를 사용한다. 단순히 임의의 지도 경로를 그리는 것이 아니라, 실제로 캠퍼스 내부를 걸으며 수집한 위도, 경도, 고도 데이터를 CSV 파일로 저장하고, 이를 기반으로 노드와 연결 구간을 구성한다.

## 2. 현재 구현 상태

현재 웹사이트는 정적 웹페이지 구조로 구현되어 있으며, 별도의 서버 프로그램 없이 HTML, CSS, JavaScript, CSV, JSON 파일만으로 동작한다.

주요 파일은 다음과 같다.

| 파일 | 역할 |
| --- | --- |
| `index.html` | 메인 경로 분석 및 추천 화면 |
| `app.js` | GNSS 데이터 처리, 노드 그래프 관리, 경로 추천 알고리즘, 지도 표시 로직 |
| `styles.css` | 전체 화면 스타일 |
| `walked_path.html` | 실측 GNSS 경로 보기 전용 화면 |
| `walked_path.js` | 실측 경로 시각화, 누적 로그 선택, 거리 요약 |
| `graph_view.html` | 노드 그래프 문서 보기 화면 |
| `graph_view.js` | `node_graph.json` 문서화 및 표 출력 |
| `node_graph.json` | 대표 노드와 연결 구간 데이터 |
| `gnss_log_2.csv` ~ `gnss_log_5.csv` | ZED-F9P로 수집한 실측 GNSS 로그 |

현재 포함된 실측 GNSS 데이터 규모는 다음과 같다.

| CSV 파일 | 데이터 행 수 |
| --- | ---: |
| `gnss_log_2.csv` | 350 |
| `gnss_log_3.csv` | 4,343 |
| `gnss_log_4.csv` | 4,295 |
| `gnss_log_5.csv` | 1,943 |
| 합계 | 10,931 |

현재 `node_graph.json`에는 대표 노드 53개와 연결 구간 201개가 저장되어 있다. 연결 구간의 거리 범위는 약 0.023 km부터 0.736 km까지이며, 평균 연결 구간 거리는 약 0.243 km이다.

## 3. 시스템 구성

전체 시스템은 다음과 같은 흐름으로 구성된다.

1. ZED-F9P 수신기로 캠퍼스 내부 이동 경로 측정
2. 측정 결과를 CSV 파일로 저장
3. CSV 파일에서 위도, 경도, 고도, 시간 데이터 파싱
4. 실측 경로 위에서 대표 노드 지정
5. 대표 노드 사이의 연결 구간 생성
6. 대표 노드와 연결 구간을 `node_graph.json`으로 저장
7. 사용자가 출발점, 도착점, 목표 거리, 난이도를 선택
8. 노드 그래프에서 조건에 맞는 후보 경로 탐색
9. 거리, 고도 변화, 중복 구간, 동선 분산도를 기준으로 점수화
10. 최종 추천 경로를 네이버 지도 위에 표시

## 4. GNSS 데이터 처리

### 4.1 CSV 입력

실측 데이터는 `gnss_log_2.csv`, `gnss_log_3.csv`, `gnss_log_4.csv`, `gnss_log_5.csv` 파일로 관리된다. 각 파일은 ZED-F9P 수신기에서 얻은 위치 로그이며, 주요 사용 컬럼은 다음과 같다.

| 컬럼 | 의미 |
| --- | --- |
| `PC_Time` | 측정 시각 |
| `Latitude_NMEA` | NMEA 형식 위도 |
| `Lat_Direction` | 북위/남위 방향 |
| `Longitude_NMEA` | NMEA 형식 경도 |
| `Lon_Direction` | 동경/서경 방향 |
| `Fix` | GNSS 위치 고정 상태 |
| `Satellites` | 사용 위성 수 |
| `Altitude_m` | 고도 |

### 4.2 NMEA 좌표 변환

CSV에 저장된 위도와 경도는 일반적인 십진수 좌표가 아니라 NMEA 형식이다. 예를 들어 위도 값은 `ddmm.mmmm`, 경도 값은 `dddmm.mmmm` 형식으로 저장된다.

코드에서는 다음 방식으로 십진수 좌표로 변환한다.

```text
degrees = floor(raw / 100)
minutes = raw - degrees * 100
decimal = degrees + minutes / 60
```

남위 또는 서경인 경우에는 부호를 음수로 바꾼다. 충북대학교 데이터는 북위, 동경이므로 일반적으로 양수 좌표가 사용된다.

### 4.3 거리 계산

두 지점 사이의 거리는 지구 곡률을 반영한 Haversine 공식을 사용한다. 코드에서는 지구 반지름을 6371 km로 두고 위도, 경도 차이를 라디안으로 변환한 뒤 두 좌표 사이의 대권 거리를 계산한다.

이 방식은 캠퍼스 내부처럼 짧은 거리에서도 충분히 안정적으로 사용할 수 있으며, 단순 평면 좌표보다 위도/경도 기반 계산에 적합하다.

### 4.4 고도 처리

고도는 CSV의 `Altitude_m` 값을 사용한다. 현재 시스템은 외부 지형 고도 데이터나 국토지리정보원 DEM 데이터를 자동으로 결합하지 않는다. 따라서 고도 변화는 ZED-F9P 수신기에서 기록된 고도값과 노드 그래프에 저장된 고도값에 의존한다.

고도 관련 지표는 다음과 같이 계산된다.

| 지표 | 의미 |
| --- | --- |
| 누적 상승 | 경로를 따라 이동하면서 고도가 올라간 변화량의 합 |
| 누적 하강 | 경로를 따라 이동하면서 고도가 내려간 변화량의 합 |
| 고도 범위 | 경로 전체의 최소 고도와 최대 고도 |

주의할 점은 GNSS 고도는 수평 위치보다 오차가 크게 나타날 수 있다는 것이다. 같은 구간을 여러 번 측정했는데도 비슷한 고도 경향이 반복된다면 프로젝트에서는 실측 데이터로 사용할 수 있지만, 특정 구간에서 비정상적인 고도 튐이 보이면 재측정 또는 보정이 필요하다.

## 5. 실측 경로 시각화

실측 GNSS 경로 보기 화면에서는 CSV 파일을 지도 위에 표시한다.

현재 지원하는 표시 방식은 다음과 같다.

| 선택 항목 | 표시 내용 |
| --- | --- |
| 실측 경로 1 | `gnss_log_2.csv` |
| 실측 경로 2 | `gnss_log_3.csv` |
| 실측 경로 3 | `gnss_log_4.csv` |
| 실측 경로 4 | `gnss_log_5.csv` |
| 실측 경로 1+2 | 1번과 2번 로그 누적 표시 |
| 실측 경로 1+2+3 | 1번, 2번, 3번 로그 누적 표시 |
| 실측 경로 1+2+3+4 | 전체 로그 누적 표시 |
| 전체 실측 경로 | 현재 포함된 모든 CSV 표시 |

네이버 지도 위에서는 실측 경로를 점과 선으로 표시한다. 점은 실제 측정 지점을 의미하고, 선은 측정 순서에 따른 이동 경로를 의미한다. 이를 통해 사용자는 어느 구간을 실제로 측정했는지 확인할 수 있다.

## 6. 대표 노드 그래프

### 6.1 대표 노드

대표 노드는 캠퍼스 내부에서 경로 추천에 사용할 주요 지점이다. 예를 들어 후문, 도서관, 자연대, 공대, 대운동장, 소운동장 등 러닝 경로를 구성할 때 기준이 되는 지점을 대표 노드로 설정한다.

각 대표 노드는 `node_graph.json`에 다음 정보를 가진다.

| 필드 | 의미 |
| --- | --- |
| `id` | 노드 ID, 예: `N001` |
| `name` | 노드 이름 |
| `lat` | 위도 |
| `lng` | 경도 |
| `altitude` | 고도 |
| `sourceIndex` | 원본 GNSS 로그에서의 인덱스 |
| `sourceLog` | 해당 노드가 만들어진 CSV 파일 |

### 6.2 연결 구간

연결 구간은 대표 노드와 대표 노드 사이를 이동할 수 있는 실측 기반 경로이다. 단순 직선 연결이 아니라, 가능하면 GNSS 로그에서 실제 이동한 좌표열을 따라 연결한다.

각 연결 구간은 다음 정보를 가진다.

| 필드 | 의미 |
| --- | --- |
| `id` | 연결 구간 ID |
| `from` | 시작 대표 노드 ID |
| `to` | 도착 대표 노드 ID |
| `distanceKm` | 구간 거리 |
| `climbM` | 구간 누적 상승 |
| `descentM` | 구간 누적 하강 |
| `altitudeRange` | 구간 고도 범위 |
| `path` | 실제 표시할 위도/경도 좌표열 |

이 연결 구간들이 모이면 캠퍼스 내부 러닝 네트워크가 된다. 즉, 대표 노드는 그래프의 정점이고 연결 구간은 그래프의 간선이다.

## 7. 연결 구간 생성 방식

연결 구간은 수동 저장 또는 자동 후보 생성을 통해 만들어진다.

### 7.1 GNSS 보조 노드 그래프

대표 노드끼리 바로 직선으로 연결하면 사람이 실제로 걸을 수 없는 경로가 생길 수 있다. 이를 줄이기 위해 실측 GNSS 좌표들을 보조 노드로 사용한다.

시스템은 실측 경로의 좌표들을 기반으로 보조 그래프를 만든다.

1. 실측 GNSS 좌표를 보조 노드로 변환
2. 같은 CSV 로그 안에서 시간 순서대로 인접한 점들을 연결
3. 서로 다른 로그라도 매우 가까운 점은 교차 연결
4. 대표 노드 주변에서 가장 가까운 보조 노드를 찾음
5. 보조 노드 그래프에서 최단 경로를 계산

이 과정에서 사용하는 핵심 함수는 `buildAuxiliaryGraph()`와 `findShortestAuxiliaryPath()`이다.

### 7.2 보조 그래프 최단 경로

대표 노드 A와 대표 노드 B를 연결할 때, 시스템은 A 주변 보조 노드와 B 주변 보조 노드를 찾고, 그 사이의 최단 경로를 탐색한다. 이때 내부적으로는 거리 누적값이 가장 작은 노드를 확장하는 방식의 최단 경로 탐색을 수행한다.

이 방식은 다익스트라 알고리즘과 유사한 구조이다. 다만 전체 프로젝트의 최종 경로 추천은 단순 다익스트라가 아니라, 이 보조 경로 계산을 포함한 그래프 기반 추천 방식이다.

## 8. 경로 추천 알고리즘

### 8.1 기본 개념

현재 경로 추천은 “최단 거리 하나를 찾는 알고리즘”이 아니다. 러닝 경로는 단순히 가장 짧은 길이 아니라 사용자가 선택한 목표 거리, 난이도, 출발점, 도착점에 맞아야 한다.

따라서 현재 시스템은 다음 방식으로 동작한다.

```text
대표 노드 그래프에서 가능한 경로 후보 생성
→ 각 후보의 거리, 고도, 반복, 동선 분산도 계산
→ 난이도 조건에 맞게 점수화
→ 점수가 가장 좋은 후보 선택
```

기술적으로는 Beam Search에 가까운 휴리스틱 탐색 방식이다.

### 8.2 탐색 방식

경로 추천 함수는 `findRecommendedGraphRoute()`이다.

탐색 과정은 다음과 같다.

1. 사용자가 선택한 출발 노드와 도착 노드를 가져온다.
2. `node_graph.json`의 연결 구간으로 인접 리스트를 만든다.
3. 출발 노드에서 시작하는 경로 상태를 만든다.
4. 연결 가능한 다음 노드를 하나씩 확장한다.
5. 목표 거리의 최대 허용 범위를 넘는 경로는 제거한다.
6. 같은 노드나 같은 간선을 반복해서 쓰는 경로는 제한한다.
7. 도착 노드에 도달한 경로를 후보로 저장한다.
8. 후보가 너무 많아지면 점수가 좋은 일부 후보만 남긴다.
9. 최종 후보 중 가장 좋은 경로를 선택한다.

이 방식은 모든 가능한 경로를 끝까지 완전탐색하지 않는다. 경로 수가 많아질수록 경우의 수가 급격히 증가하기 때문에, 실시간 웹페이지에서 동작하기 위해 가능성이 낮은 후보를 중간에 제거한다.

### 8.3 목표 거리 범위

현재 목표 거리는 최대 5 km로 제한되어 있다. 코드상 `MAX_TARGET_DISTANCE = 5`로 설정되어 있다.

경로 탐색에서는 목표 거리와 정확히 같은 거리만 허용하지 않고, 일정 범위를 허용한다. 현재 거리 허용 범위는 대략 다음과 같이 계산된다.

```text
최소 거리 = max(0.75 km, 목표 거리 × 0.78)
최대 거리 = min(5.08 km, 목표 거리 + 0.12 km)
```

따라서 5 km를 선택하면 약 3.9 km 이상, 5.08 km 이하의 후보가 탐색 범위에 들어갈 수 있다. 이후 점수 계산에서 목표 거리와 더 가까운 경로가 유리해진다.

### 8.4 점수 계산

후보 경로의 점수는 `scoreGraphRoute()`에서 계산한다. 점수가 낮을수록 좋은 경로이다.

점수에 반영되는 주요 요소는 다음과 같다.

| 요소 | 설명 |
| --- | --- |
| 거리 오차 | 목표 거리보다 너무 짧거나 긴 정도 |
| 반복 노드 수 | 같은 대표 노드를 반복해서 지나는 정도 |
| 반복 간선 수 | 같은 연결 구간을 반복해서 지나는 정도 |
| 동선 분산도 | 캠퍼스 여러 구역을 넓게 사용하는 정도 |
| 움직임 품질 | 급격한 꺾임, 짧은 왕복, 좁은 구역 반복 여부 |
| 누적 상승/하강 | 경사도 난이도 판단에 사용 |
| 고도 밀도 | 거리 대비 고도 변화량 |

### 8.5 난이도 반영

난이도는 쉬움, 중간, 어려움으로 구분한다.

| 난이도 | 경로 선택 기준 |
| --- | --- |
| 쉬움 | 고도 변화가 적고, 누적 상승이 낮은 후보를 선호 |
| 중간 | 고도 변화가 너무 낮거나 높지 않은 균형 후보를 선호 |
| 어려움 | 누적 상승과 고도 변화가 큰 후보를 선호 |

쉬움은 `climbScore`가 낮은 후보가 유리하고, 어려움은 `climbScore`가 높은 후보가 유리하다. 중간은 후보들 중 고도 변화가 중앙값에 가까운 경로를 선호한다.

### 8.6 중복 동선 억제

러닝 경로에서 같은 구간을 반복해서 왕복하면 발표용으로 보기에도 좋지 않고 실제 러닝 코스로도 어색하다. 이를 줄이기 위해 다음 제한을 둔다.

| 제한 | 내용 |
| --- | --- |
| 대표 노드 방문 제한 | 기본적으로 같은 중간 노드를 두 번 이상 방문하지 않도록 제한 |
| 연결 구간 사용 제한 | 같은 연결 구간을 반복 사용하지 않도록 제한 |
| 즉시 되돌아가기 제한 | 방금 온 길을 바로 되돌아가는 경로를 억제 |
| 집중 구역 패널티 | 좁은 구역 안에서만 도는 후보에 불이익 |
| 분산 보너스 | 캠퍼스를 넓게 사용하는 후보에 보너스 |

이 때문에 추천 결과는 단순 최단거리보다 캠퍼스 전체를 더 넓게 사용하는 방향으로 조정된다.

## 9. 지도 표시 방식

### 9.1 네이버 지도 API

웹사이트는 네이버 지도 JavaScript API를 사용한다. 사용자가 Client ID를 입력하고 지도 불러오기를 누르면 네이버 지도 위에 실측 경로, 대표 노드, 연결 구간, 추천 경로가 표시된다.

네이버 지도 API 키는 코드에 고정 저장하지 않고 사용자가 입력하도록 구성되어 있다. 이는 API 키 노출을 줄이기 위한 방식이다.

### 9.2 실측 GNSS 경로 표시

실측 GNSS 경로는 빨간색 점과 선으로 표시된다. 점은 실제 측정 좌표이고, 선은 시간 순서대로 이어진 이동 경로이다.

### 9.3 연결 노드 표시

대표 노드는 파란색 원으로 표시된다. 연결 구간은 노란색 점과 선으로 표시된다. 이를 통해 현재 노드 그래프가 어느 구간을 연결하고 있는지 확인할 수 있다.

### 9.4 추천 경로 표시

추천 경로는 초록색 점과 선으로 표시된다. 추천 경로의 주요 대표 노드는 방문 순서 번호와 함께 표시된다. 예를 들어 1, 2, 3, 4처럼 순서가 표시되므로 사용자는 어느 노드를 어떤 순서로 지나야 하는지 확인할 수 있다.

## 10. 웹 저장 방식

대표 노드와 연결 구간은 브라우저의 `localStorage`에도 저장된다. 사용자가 웹사이트에서 노드 그래프를 수정하면 로컬 브라우저에 저장되며, 내보내기 기능을 통해 `node_graph.json` 파일로 저장할 수 있다.

GitHub Pages에 배포할 때는 최신 `node_graph.json` 파일을 함께 올려야 외부 사용자도 같은 노드 그래프를 볼 수 있다.

현재 메인 페이지는 기본적으로 `node_graph.json`을 불러온다. 만약 브라우저에 저장된 로컬 그래프가 비어 있거나, 포함된 `node_graph.json`이 더 많은 노드/연결 구간을 가지고 있으면 기본 파일을 불러와 사용한다.

## 11. 배포 방식

프로젝트는 정적 웹사이트이므로 GitHub Pages에 배포할 수 있다.

배포 구조는 다음과 같다.

```text
GitHub Repository
→ main 브랜치
→ HTML/CSS/JS/CSV/JSON 파일 업로드
→ GitHub Pages 활성화
→ 외부 사용자가 URL로 접속
```

현재 프로젝트 주소는 다음과 같다.

```text
https://github.com/myoungbong/campus-run-navigator.git
```

GitHub Pages 주소는 다음 형식이다.

```text
https://myoungbong.github.io/campus-run-navigator/
```

GitHub Pages는 정적 파일을 제공하므로 웹사이트를 항상 켜두기 위한 별도의 서버 실행이 필요 없다. 다만 파일 수정 후 GitHub Pages에 반영되기까지 약간의 시간이 걸릴 수 있으며, 브라우저 캐시 때문에 `Ctrl + F5` 새로고침이 필요할 수 있다.

## 12. 개인정보 및 데이터 공개 주의

GNSS CSV 파일에는 위치와 시간이 포함된다. 이는 특정 시간에 사용자가 어디를 이동했는지 알 수 있는 데이터이므로 개인정보 또는 위치정보로 취급될 수 있다.

프로젝트에서 팀원끼리 공유하는 목적이라면 공개 저장소에 올릴 수 있지만, 다음 사항을 주의해야 한다.

1. 측정 날짜와 시간이 그대로 공개될 수 있다.
2. 반복 측정 패턴을 통해 사용자의 생활 동선이 추정될 수 있다.
3. 원본 CSV는 발표 후 필요하면 비공개 저장소로 옮기는 것이 안전하다.
4. 공개 발표용으로는 원본 시간 값을 제거하거나 샘플링한 데이터만 사용하는 방법도 있다.

## 13. 현재 한계점

현재 시스템은 실제 프로젝트 시연이 가능한 수준으로 구현되어 있지만, 다음과 같은 한계가 있다.

### 13.1 GNSS 오차

건물 사이, 나무 주변, 높은 구조물 근처에서는 위성 신호가 반사되거나 약해질 수 있다. 이로 인해 위치가 실제 보행 경로에서 벗어나거나 고도가 튀는 문제가 발생할 수 있다.

### 13.2 고도 정확도

ZED-F9P는 수평 위치 정확도가 좋지만, 고도는 수평 위치보다 불안정할 수 있다. 특히 짧은 구간에서 고도가 갑자기 크게 변하면 실제 경사보다 GNSS 오차일 가능성이 있다.

### 13.3 노드 그래프 의존성

경로 추천은 `node_graph.json`의 대표 노드와 연결 구간 품질에 크게 의존한다. 연결 구간이 부족하거나 잘못 연결되면 추천 경로도 부자연스러워질 수 있다.

### 13.4 보행 가능 여부

현재 시스템은 네이버 지도 위의 도로망 데이터를 직접 분석하지 않는다. 따라서 보행 가능 여부는 실측 GNSS 경로와 사용자가 만든 연결 구간에 의존한다.

### 13.5 최적해 보장

현재 추천 알고리즘은 완전탐색이나 수학적 최적화 알고리즘이 아니라 휴리스틱 Beam Search 방식이다. 실시간 웹 환경에서 빠르게 동작하도록 후보를 제한하기 때문에 항상 전역 최적해를 보장하지는 않는다. 대신 러닝 코스 추천이라는 목적에 맞게 거리, 경사도, 반복, 동선 분산도를 균형 있게 반영한다.

## 14. 발표용 알고리즘 설명

발표에서는 다음과 같이 설명할 수 있다.

> 본 프로젝트는 충북대학교 내부를 직접 이동하며 수집한 GNSS 로그를 기반으로 러닝 경로를 추천하는 웹 시스템입니다. ZED-F9P 수신기로 측정한 위도, 경도, 고도 데이터를 CSV로 저장하고, 이를 지도 위에 시각화했습니다. 이후 캠퍼스 주요 지점을 대표 노드로 지정하고, 실제 GNSS 이동 경로를 기반으로 대표 노드 사이의 연결 구간을 구성했습니다.
>
> 경로 추천은 대표 노드를 정점, 연결 구간을 간선으로 하는 그래프에서 수행됩니다. 사용자가 목표 거리, 출발점, 도착점, 난이도를 선택하면, 시스템은 가능한 경로 후보를 탐색하고 거리 오차, 누적 고도 변화, 반복 구간, 동선 분산도를 기준으로 점수를 계산합니다. 쉬움 난이도는 고도 변화가 적은 경로, 어려움 난이도는 고도 변화가 큰 경로를 우선하며, 최종적으로 점수가 가장 좋은 후보를 추천합니다.
>
> 단순 최단거리 알고리즘과 달리 러닝 코스는 목표 거리와 난이도를 만족해야 하므로, 본 시스템은 실측 GNSS 기반 노드 그래프와 휴리스틱 탐색을 결합한 방식으로 구현되었습니다.

## 15. 향후 개선 방향

추가로 개선할 수 있는 방향은 다음과 같다.

1. 고도 데이터 보정  
   국토지리정보원 DEM, 공공 고도 데이터, 반복 측정 평균값 등을 활용해 고도 튐을 줄일 수 있다.

2. 경로 추천 품질 향상  
   후보 경로의 급격한 꺾임, 좁은 구역 반복, 지나치게 긴 우회 구간을 더 강하게 제한할 수 있다.

3. 노드 그래프 자동화  
   GNSS 로그에서 자동으로 교차점, 분기점, 회전 지점을 감지해 보조 노드 또는 대표 노드 후보를 만들 수 있다.

4. 사용자 설정 저장  
   목표 거리, 선호 난이도, 자주 쓰는 출발점 등을 저장해 개인화할 수 있다.

5. 모바일 최적화  
   실제 러닝 중 스마트폰에서 보기 쉽도록 버튼 크기, 지도 영역, 경로 안내 문구를 모바일 중심으로 개선할 수 있다.

6. 공개 데이터 보안 처리  
   CSV에서 시간 정보를 제거하거나 좌표를 단순화한 공개용 데이터셋을 따로 만들 수 있다.

## 16. 결론

Campus Run Navigator는 단순 지도 표시 웹페이지가 아니라, GNSS 실측 데이터 수집, NMEA 좌표 변환, 거리 및 고도 계산, 대표 노드 그래프 구성, 실측 기반 연결 구간 생성, 조건 기반 경로 추천까지 포함하는 위성항법 응용 프로젝트이다.

현재 구현은 충북대학교 내부를 대상으로 하며, 직접 수집한 GNSS 데이터를 바탕으로 캠퍼스 러닝 코스를 추천한다. 특히 목표 거리와 난이도 조건을 함께 고려하고, 추천 경로를 네이버 지도 위에 시각적으로 표시한다는 점에서 위성항법 데이터의 실제 활용 사례로 설명할 수 있다.

